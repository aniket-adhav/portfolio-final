{
  "buildCommand": "node ./scripts/draco.cjs && pnpm run build",
  "installCommand": "pnpm install --ignore-scripts",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["build/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
